name: pdfpwd-18

services:
  db:
    image: postgres:16
    container_name: pdfpwd-pg-18
    environment:
      POSTGRES_USER: odoo
      POSTGRES_PASSWORD: odoo
      POSTGRES_DB: postgres
    volumes:
      - db-data:/var/lib/postgresql/data

  odoo:
    build: .
    container_name: pdfpwd-odoo-18
    depends_on:
      - db
    ports:
      - "1816:8069"
    environment:
      HOST: db
      USER: odoo
      PASSWORD: odoo
    volumes:
      - odoo-data:/var/lib/odoo
      - ./no_pdf_password_protection:/mnt/extra-addons/no_pdf_password_protection

volumes:
  db-data:
  odoo-data:
