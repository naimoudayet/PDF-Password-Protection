name: pdfpass-19

services:
  db:
    image: postgres:16
    container_name: pdfpass-pg-19
    environment:
      POSTGRES_USER: odoo
      POSTGRES_PASSWORD: odoo
      POSTGRES_DB: postgres
    volumes:
      - db-data:/var/lib/postgresql/data

  odoo:
    build: .
    container_name: pdfpass-odoo-19
    depends_on:
      - db
    ports:
      - "1819:8069"
    environment:
      HOST: db
      USER: odoo
      PASSWORD: odoo
    volumes:
      - odoo-data:/var/lib/odoo
      - ./no_pdf_password_protection:/mnt/extra-addons/no_pdf_password_protection

volumes:
  db-data:
  odoo-data:
